// PART 3: DATA TYPES

/**
 * INSTRUCTIONS:
 *  1. Create datatypes.js file and use the JavaScript typeof operator 
 *     to check different data types. Check the data type of each variable.
 */

import { userName, isOnline, bloodType, netWorth } from "./variable";

console.log(typeof(userName));
console.log(typeof(isOnline));
console.log(typeof(bloodType));
console.log(typeof(netWorth));